snippet for
options head
	for (var ${1:i} = 0; $1 < ${2:#:Things}.length; $1++) {
		${0:TARGET}
	}

snippet switch
	switch (${1:expression}) {
		case ${2:value}:
			${3:statement}
			break;
	}

snippet case
options indent
	case ${1:value}:
		${2:statement}
		break;

snippet     cl
abbr        console.log(..);
options     head
	console.log(${0:TARGET});
